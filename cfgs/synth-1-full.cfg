<include> = synth-base.cfg

identifier = synth-1-full
filename = synth-1.data
# storage type (dynamic,fixed,ring)
storage_class = fixed
# whether to save a particle or not (none,one,all)
save_particle = one
